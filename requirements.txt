# --- Core runtime (matches pyproject [project.dependencies])
requests>=2.28.0
urllib3>=1.26.0
httpx>=0.26.0
chardet>=5.2.0

# --- CLI UX (optional in production, but useful for dev)
rich>=13.0.0
colorama>=0.4.6; sys_platform == "win32"

# --- Headless (optional; prefer `pip install .[headless]`)
# playwright>=1.36.0

# --- Dev / QA
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0

# --- Docs (optional)
sphinx>=6.0.0
sphinx-rtd-theme>=1.0.0

# --- Packaging
setuptools>=69
wheel>=0.41
twine>=4.0.0

